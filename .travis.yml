language: node_js
node_js:
  - '10'

branches:
  only:
  - source

before_install:
- git config --global user.name "aaron"
- git config --global user.email "qcqilin@gmail.com"
- sed -i'' "s~https://github.com/aaro-n/aaro-n.github.io.git~https://${9dc928ee9179586c10fb007af1be6f4d19fbdf6e}@github.com/aaro-n/aaro-n.github.io.git~" _config.yml

install:
- npm install hexo-cli -g
- npm install

script:
- npm run deploy
